# PDF label templates

Place the production waybill template here as `waybill_100x150.pdf` or override `PRINT_PDF_TEMPLATE_PATH` in environment-specific config.

The backend production print mode `pdf_template` renders label variables into a final PDF and sends that PDF to CUPS without browser print dialogs.
